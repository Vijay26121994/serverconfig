server:
  port: 8070

eureka:
  instance:
    hostname: localhost
  client:
    fetch-registry: false # server itself does not need to fetch registry
    registerWithEureka: false # server itself does not need to register
    serviceUrl:
      defaultZone: http://${eureka.instance.hostname}:${server.port}/eureka/
